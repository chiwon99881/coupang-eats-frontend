import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import React from 'react';

export const Loading: React.FunctionComponent = () => {
  return <FontAwesomeIcon icon="spinner" size="5x" />;
};
